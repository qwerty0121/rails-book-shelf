<style>
  .back {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.875rem;
    margin-bottom: 1.25rem;
  }

  .back:hover {
    color: var(--text);
  }

  h1 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
  }

</style>
<div class="container">
  <%= link_to "← 一覧に戻る", books_path, class: "back" %>
  <h1>蔵書登録</h1>

  <%= render "form", book: @book, submit_label: "登録する", cancel_url: books_path %>
</div>
